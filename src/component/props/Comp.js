import React from 'react'

const Comp = (props) => {
    return (
        <div>

            <h1>{props.name}</h1>
            <h1>{props.age}</h1>
            <p> Helo there this is from comp component page</p>


        </div>
    )
}

export default Comp